<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.0</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.tmt</groupId>
	<artifactId>TMLibrary</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>TMLibrary</name>
	<description>TMLibrary admin system backend</description>

	<properties>
		<java.version>25</java.version>
	</properties>

	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>

		<!-- Bean validation (@NotBlank, @Size, @Valid 等), spring-boot 4.x 单独成包 -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>

		<!--
		  邮件 —— 目前只服务于「忘记密码」的重置链接。

		  加了 starter 不等于就会发邮件:Spring Boot 的
		  MailSenderAutoConfiguration 带 @ConditionalOnProperty(spring.mail.host),
		  没配 SMTP 主机时**根本不会创建 JavaMailSender Bean**,
		  于是邮件服务的实现自动退回 LoggingMailService(把重置链接打进日志)。
		  所以本地开发不配 SMTP 也能跑通整个重置流程。
		-->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-mail</artifactId>
		</dependency>

		<!-- MyBatis (Spring Boot 3+ Jakarta-aware starter; no MyBatis-Plus) -->
		<dependency>
			<groupId>org.mybatis.spring.boot</groupId>
			<artifactId>mybatis-spring-boot-starter</artifactId>
			<version>3.0.5</version>
		</dependency>

		<!-- MySQL JDBC driver -->
		<dependency>
			<groupId>com.mysql</groupId>
			<artifactId>mysql-connector-j</artifactId>
			<version>9.6.0</version>
			<scope>runtime</scope>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-devtools</artifactId>
			<scope>runtime</scope>
			<optional>true</optional>
		</dependency>

		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
    		<groupId>org.springframework.security</groupId>
    		<artifactId>spring-security-crypto</artifactId>
		</dependency>

		<!-- API：编译期写代码用，不加scope -->
		<dependency>
    		<groupId>io.jsonwebtoken</groupId>
    		<artifactId>jjwt-api</artifactId>
    		<version>0.12.6</version>
		</dependency>
		<!-- impl：运行时实现，scope=runtime，编译看不到类，只有运行加载 -->
		<dependency>
    	<groupId>io.jsonwebtoken</groupId>
    		<artifactId>jjwt-impl</artifactId>
    		<version>0.12.6</version>
    		<scope>runtime</scope>
		</dependency>
		<!-- jackson序列化，runtime，SpringBoot自带jackson，刚好匹配 -->
		<dependency>
    		<groupId>io.jsonwebtoken</groupId>
    		<artifactId>jjwt-jackson</artifactId>
    		<version>0.12.6</version>
    		<scope>runtime</scope>
		</dependency>

		<!-- logback -->
		<dependency>
			<groupId>ch.qos.logback</groupId>
			<artifactId>logback-classic</artifactId>
		</dependency>
		<!-- AOP -->
		<dependency>
    		<groupId>org.springframework.boot</groupId>
			<!--2026 年就改名字了-->
    		<artifactId>spring-boot-starter-aspectj</artifactId>
			<!-- <version>4.1.0</version> -->
			
		</dependency>
		<!-- Actuator + Micrometer:暴露健康检查与业务指标(库存漂移/补偿失败计数) -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>

		<!--spring-boot 的redis 依赖-->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-redis</artifactId>
		</dependency>
		<!-- 如果需要连接池！Lettuce/Jedis 都需要 commons‑pool2 连接池依赖 -->
		<dependency>
			<groupId>org.apache.commons</groupId>
			<artifactId>commons-pool2</artifactId>
		</dependency>

		<!-- ============================================================
		     Elasticsearch 客户端
		   ============================================================
		   Spring Boot 4 起官方不再把 ES 客户端纳入默认 dependency management —— 直接锁 9.5.4,
		   跟本地 docker 集群同主版本(9.x 是当前 LTS 路径)。
		   客户端与集群主版本不一致会出现 "no compatible version" 类错误。

		   选了 Elasticsearch Java Client(9.x 取代 HLRC 的现代官方客户端)。
		   从 9.x 起 elasticsearch-java 直接吃进了底层 transport,不再单独发布
		   elasticsearch-rest-client 模块 —— 所以下面只声明这一个 artifact。
		   HttpHost/RestClient 等类型就在这个 jar 里。 -->
		<dependency>
			<groupId>co.elastic.clients</groupId>
			<artifactId>elasticsearch-java</artifactId>
			<version>9.5.4</version>
		</dependency>
		<!--
		     Apache HttpClient 5 —— rest5-client 的运行时依赖,pom 里虽然声明了但
		     transitive 默认 scope 不是 compile,编译期找不到 org.apache.hc.*5.* 的类。
		     必须显式声明,且不锁版本(rest5-client 9.5.4 会用 5.6.1,本地 Maven 缓存里就有)。
		     想完全避坑可以锁 5.6.1,但有版本不匹配的风险 —— 等首跑通了再定。
		-->
		<dependency>
			<groupId>org.apache.httpcomponents.client5</groupId>
			<artifactId>httpclient5</artifactId>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<executions>
					<execution>
						<id>default-compile</id>
						<phase>compile</phase>
						<goals>
							<goal>compile</goal>
						</goals>
						<configuration>
							<annotationProcessorPaths>
								<path>
									<groupId>org.projectlombok</groupId>
									<artifactId>lombok</artifactId>
								</path>
							</annotationProcessorPaths>
						</configuration>
					</execution>
					<execution>
						<id>default-testCompile</id>
						<phase>test-compile</phase>
						<goals>
							<goal>testCompile</goal>
						</goals>
						<configuration>
							<annotationProcessorPaths>
								<path>
									<groupId>org.projectlombok</groupId>
									<artifactId>lombok</artifactId>
								</path>
							</annotationProcessorPaths>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
			<!--使用方法 mvn clean test jacoco:report -->
            <groupId>org.jacoco</groupId>
            <artifactId>jacoco-maven-plugin</artifactId>
            <version>0.8.13</version>
            <executions>
                <execution>
                    <goals>
                        <goal>prepare-agent</goal>
                        <goal>report</goal>
                    </goals>
                </execution>
            </executions>
		</plugin>
		<!--
			Failsafe 跑 *IT.java 集成测试(走真 SQL + H2)
			- surefire 跑 *Test.java 单元测试
			- failsafe 跑 *IT.java 集成测试
			- mvn test 只跑单测;mvn verify 跑单测 + 集成测试
		-->
		<plugin>
			<groupId>org.apache.maven.plugins</groupId>
			<artifactId>maven-failsafe-plugin</artifactId>
			<executions>
				<execution>
					<goals>
						<goal>integration-test</goal>
						<goal>verify</goal>
					</goals>
				</execution>
			</executions>
        </plugin>
		</plugins>
	</build>

</project>